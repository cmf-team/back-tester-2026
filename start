#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ ! -x "$SCRIPT_DIR/run" ]]; then
  echo "Error: ./run script is missing or not executable." >&2
  echo "Run: chmod +x run" >&2
  exit 1
fi

exec "$SCRIPT_DIR/run" "$@"